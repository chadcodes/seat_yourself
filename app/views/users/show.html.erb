<h1>User # <%= @user[:name] %></h1>


<p><%= image_tag @user.avatar.url(:medium) %></p>

<ul>
	<li><%= link_to @user[:name], user_path(@user) %></li>
	<li><%= @user[:email]%></li>

</ul>

<h4>Reservations</h4>
<p>Number of reservations = <%=  @user_reservations.count %></p>

<%  @all_user_reservations.each do |p| %>
	
	<p><%= p.restaurant_name %></p>
	<img src="<%= p.photo_url %>" widht="100" height="100" alt="">
	<p><%= p.street_address %></p>
	<p><%= p.postal_code %></p>
	<p>----------------------------</p>

<% end %>

<ul>
	<li><%= link_to 'Edit Profile', edit_user_path(@user) %></li>
	<li><%= link_to "Delete Account", user_path(@user) , method: :delete, data: { confirm: 'Are you sure?' } %></li>
	<li><%= link_to "Back", reservation_path(@user) %></li>
</ul>


